var util = require("util");

var Type = function Type(typeData) {
    if (!barData.__options__) {
        throw new Error("No options field in Type: " + util.inspect(typeData));
    }
}

module.exports = Bar;